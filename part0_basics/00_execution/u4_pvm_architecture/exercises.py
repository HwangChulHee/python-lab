"""u4 유제"""
import dis
import sys

# ═══════════════════════════════════════════════════
# 유제 1. 손 트레이스
# ═══════════════════════════════════════════════════
def mystery(a, b):
    c = a * b
    d = c + a
    return d

# dis를 실행하지 말고, 먼저 바이트코드를 직접 예상해서 아래에 쓸 것.
# 형식: 명령어 / 실행 후 스택 상태
# (README의 x = a + b 트레이스를 참고)
"""
내 예상:

"""
print("=== 정답 확인 ===")
dis.dis(mystery)
# 예상과 다른 부분과 그 이유:
#   →


# ═══════════════════════════════════════════════════
# 유제 2. u2 유제 3의 manual_style을 스택으로 재해석
# ═══════════════════════════════════════════════════
# u2에서 본 manual_style 바이트코드:
#   LOAD_CONST <code f> / MAKE_FUNCTION / STORE_FAST f
#   LOAD_GLOBAL logger / LOAD_FAST f / CALL 1 / STORE_FAST f
#
# 각 줄 실행 후의 스택 상태를 써서, deco_style과 달리
# "장식 전 f가 이름에 묶이는 순간"이 어디인지 짚어라:
"""
트레이스:

장식 전 f가 이름에 묶이는 지점:
"""


# ═══════════════════════════════════════════════════
# 유제 3. 트레이스백 = 프레임 체인 검증
# ═══════════════════════════════════════════════════
def level3():
    raise ValueError("바닥")

def level2():
    level3()

def level1():
    level2()

# (a) 먼저 예측: level1()을 호출하면 트레이스백에 함수 이름이
#     어떤 순서로 나올까?
#   예측 →
#
# (b) 실행해서 확인 (주석 해제):
# level1()
#
# (c) 예제 2의 프레임 체인 출력과 순서가 같은가 반대인가? 왜?
#   →