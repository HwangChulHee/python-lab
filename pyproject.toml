[project]
name = "python-lab"
version = "0.1.0"
description = "파이썬 동작 원리 딥다이브 — 백엔드 개발자 관점"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["."]
python_files = ["exercises.py", "test_*.py"]
