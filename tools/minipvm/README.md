# minipvm — pvmlab으로 대체됨

이 디렉터리는 `tools/pvmlab/`로 대체되었습니다. pvmlab은 비재귀 평가 루프,
객체 인스펙터(코드/함수 객체 속성 + diff), 장별 opcode/데모 분리 구조를 갖춘
후속 버전입니다. 새 작업은 모두 `tools/pvmlab/`에서 진행하세요.

여기 파일들은 참고용으로 남겨 둡니다. 필요 없으면 직접 삭제해도 됩니다.
